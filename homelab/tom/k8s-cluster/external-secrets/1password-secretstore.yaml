---
apiVersion: external-secrets.io/v1
kind: SecretStore
metadata:
  name: 1password-sdk
spec:
  provider:
    onepasswordSDK:
      vault: K8s - Tom
      auth:
        serviceAccountSecretRef:
          name: onepassword-connect-token-tom
          key: token
      integrationInfo:
        name: integration-info
        version: v1
